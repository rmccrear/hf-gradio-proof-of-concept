# Optional: needed only when calling private Spaces or authenticated APIs.
HF_TOKEN=
HF_MODEL=meta-llama/Llama-3.1-8B-Instruct:novita
HF_PROVIDER=

# Frontend default target. For local development, run the backend on 7860.
VITE_GRADIO_APP_URL=http://127.0.0.1:7860
